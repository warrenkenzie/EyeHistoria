@model List<EyeHistoria.Models.Diagnosis_symptoms>

<h4>Diagnosis_symptoms</h4>
<hr />

@foreach (var item in Model)
{
    <div>
        <dl class="row">
            <dd class="col-sm-10">
                @Html.DisplayFor(model => item.SymptomName)
                <a asp-action="ViewFollowUp" asp-route-id="@item.Diagnosis_symptomsID">View More</a>
            </dd>
        </dl>
    </div>
    <hr />
}

<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = item.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
